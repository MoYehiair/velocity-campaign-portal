# Public configuration; safe to expose in the browser through /api/config.
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Privileged: server and provisioning scripts only. Never use in NEXT_PUBLIC_*.
SUPABASE_SERVICE_ROLE_KEY=
MESSAGING_API_KEY=
MESSAGING_BASE_URL=https://dispatcher-production-72fc.up.railway.app
APP_URL=http://localhost:3000
# Generate using: openssl rand -hex 32
CRON_SECRET=
# Needed only by the account provisioning script.
DEMO_GOOGLE_EMAIL=
